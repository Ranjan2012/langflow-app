services:
  - type: web
    name: langflow-docker
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: LANGFLOW_HOST
        value: 0.0.0.0
      - key: LANGFLOW_PORT
        value: 7860
